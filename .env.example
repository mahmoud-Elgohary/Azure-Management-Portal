# WTS Azure Cloud Manager — environment configuration
# Copy to .env and fill in real values (run: python setup_env.py)

# ── DATA service principal (Azure-Reports-MSGraph) ──────────────────────────
# Used for ARM / Cost Management / Log Analytics reads.
# AZURE_CLIENT_ID = Application (client) ID from the app registration Overview,
#                   NOT the service-principal object ID.
# Required RBAC on subscription 484960a1-...:
#   • Reader
#   • Cost Management Reader
#   • Log Analytics Data Reader

AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_CLIENT_SECRET=your-azure-reports-msgraph-secret-here
AZURE_SUBSCRIPTION_IDS=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ── SIGN-IN app (WTS-Azure-Manager app registration) ────────────────────────
# Used only for the MSAL Authorization Code browser-login flow.
# This app must have:
#   Web redirect URI: https://192.168.1.115:8444/auth/callback
#   Delegated permissions: openid, profile, email (admin-consented)
# If AUTH_CLIENT_ID == AZURE_CLIENT_ID, the same app handles both.

AUTH_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AUTH_CLIENT_SECRET=your-sign-in-app-secret-here

# ── Flask / app settings ─────────────────────────────────────────────────────
# Flask secret key — auto-generated by setup_env.py
FLASK_SECRET_KEY=replace-with-secrets-token-hex-32-output

# Port for this app (must differ from other apps on this VM; O365 app uses 5000)
APP_PORT=8050

# How often the background sync job pulls from Azure (minutes)
SYNC_INTERVAL_MINUTES=60

# ── Entra ID sign-in (Authorization Code flow) ──────────────────────────────
# Auto-derived by setup_env.py from AZURE_TENANT_ID — set manually if overriding
AUTHORITY=https://login.microsoftonline.com/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Redirect URI path registered in the Entra app registration
AUTH_REDIRECT_PATH=/auth/callback

# Optional: restrict access to members of this group (object ID GUID).
# Leave blank to allow any user in the tenant (rely on "Assignment required" in Entra).
ALLOWED_GROUP_ID=
